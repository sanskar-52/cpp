// Problem Statement : Given a target node data and a root of binary tree.If the target is set on fire, determine the shortest amount of time needed to burn the entire binary tree.
// It is known that in 1 second all nodes connected to a given node get burned.That is its left child, right child, and parent.

#include <bits/stdc++.h>
#include "../TreeNode.h"
using namespace std;

class Solution
{
public:
  int minTime(TreeNode *root) {}
};